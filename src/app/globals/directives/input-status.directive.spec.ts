import { InputStatusDirective } from './input-status.directive';

describe('InputStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new InputStatusDirective();
    expect(directive).toBeTruthy();
  });
});
